#include<iostream>
#include<vector>
using namespace std;
class solution{
    public:
    vector<int> two sum(<vector<int> & nums int target)
    result{0,1};
    int a,b;
    for(int i=0; i<nums.size(); i++)
    {
        a=nums[i];
        for(j=i+1; j<nums.size,j++)
        b=nums[j];
        if(a+b==target)
        return(i,j);
        break;

    }

};
int main()
